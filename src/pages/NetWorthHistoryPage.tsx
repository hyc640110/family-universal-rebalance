import { useMemo, useState } from 'react';
import PageFrame from './PageFrame';
import { deriveHistoryStats, historyForRange, normalizeNetWorthHistory, type HistoryRange, type NetWorthSnapshot } from '../lib/netWorthHistory';
import ToolQuickNavigation from '../components/ToolQuickNavigation';
const money=(value:number|null)=>value===null||!Number.isFinite(value)?'—':`${value>0?'+':value<0?'-':''}${(Math.abs(value)/10000).toLocaleString('zh-TW',{maximumFractionDigits:1})} 萬元`;
const percent=(value:number|null)=>value===null||!Number.isFinite(value)?'—':`${(value*100).toFixed(1)}%`;
const tone=(value:number|null)=>value===null?'hold':value>0?'good':value<0?'bad':'hold';
export default function NetWorthHistoryPage({history}:{history?:NetWorthSnapshot[]}) { const [range,setRange]=useState<HistoryRange>('30d'); const all=useMemo(()=>normalizeNetWorthHistory(history),[history]); const rows=useMemo(()=>historyForRange(all,range),[all,range]); const stats=useMemo(()=>deriveHistoryStats(all),[all]); const values=rows.map(row=>row.netWorth); const min=Math.min(...values,0),max=Math.max(...values,1); const points=rows.map((row,index)=>`${rows.length<2?50:index/(rows.length-1)*100},${100-(row.netWorth-min)/(max-min||1)*100}`).join(' '); const cards=[['今日增加',stats.todayChange,false],['本月增加',stats.monthChange,false],['今年增加',stats.yearChange,false],['歷史最高淨資產',stats.highestNetWorth,false],['最大回撤',stats.maxDrawdown,true]] as const; return <PageFrame page="tools" title="淨資產歷史中心" description="每日快照自動統計資產、現金、負債與淨資產變化；不記錄交易流水。"><section className="history-hero"><div><p className="eyebrow">V4.1 每日快照</p><h2>淨資產趨勢</h2><p>每天最多保留一筆快照；同一天再次更新時覆蓋當天資料。</p></div><strong>{stats.latest?.date??'尚無快照'}</strong></section><section className="history-stats">{cards.map(([label,value,isPercent])=><article key={label}><small>{label}</small><strong className={tone(value)}>{isPercent?percent(value):money(value)}</strong></article>)}</section><section className="card"><div className="history-range">{([['7d','7 天'],['30d','30 天'],['90d','90 天'],['1y','1 年'],['all','全部']] as const).map(([value,label])=><button type="button" key={value} className={range===value?'active':''} onClick={()=>setRange(value)}>{label}</button>)}</div>{rows.length===0?<div className="analytics-empty"><p>尚無淨資產歷史資料</p><span>開啟 App、更新報價或修改資產／借款資料時，系統會以當日資料覆蓋快照。</span></div>:<><div className="history-chart" aria-label="淨資產歷史趨勢圖"><svg viewBox="0 0 100 100" preserveAspectRatio="none"><polyline points={points} fill="none" stroke="currentColor" strokeWidth="2" vectorEffect="non-scaling-stroke" /></svg></div><div className="history-legend"><span>淨資產趨勢</span><b>{money(rows.at(-1)?.netWorth??null)}</b></div></>}</section><section className="history-grid">{rows.slice().reverse().map(row=><article key={row.date}><header><strong>{row.date}</strong><b>{money(row.netWorth)}</b></header><div><p><span>總資產</span><b>{money(row.totalAssets)}</b></p><p><span>投資市值</span><b>{money(row.investmentValue)}</b></p><p><span>現金</span><b>{money(row.cash)}</b></p><p><span>負債</span><b>{money(row.debt)}</b></p></div></article>)}</section><ToolQuickNavigation current="net-worth-history" /></PageFrame>; }
