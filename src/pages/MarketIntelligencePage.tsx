import { RefreshCw } from 'lucide-react';
import PageFrame from './PageFrame';
import { formatMarketTime, marketTone, statusLabel, type MarketDataPoint, type MarketSnapshot } from '../lib/marketData';
import { VISIBLE_MARKET_SECTIONS, visibleMarketSnapshot } from '../lib/marketSections';
const value = (item: MarketDataPoint) => item.value === null ? '—' : `${item.value.toLocaleString('zh-TW', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}${item.unit ?? ''}`;
const change = (item: MarketDataPoint) => item.change === null ? '變化資料不足' : `${item.change > 0 ? '+' : ''}${item.change.toLocaleString('zh-TW', { maximumFractionDigits: 2 })}${item.changePct === null ? '' : `（${item.changePct > 0 ? '+' : ''}${item.changePct.toFixed(2)}%）`}`;
export default function MarketIntelligencePage({ snapshot, isRefreshing, refreshMessage, onRefresh }: { snapshot: MarketSnapshot; isRefreshing: boolean; refreshMessage?: string; onRefresh: () => void }) { const visibleSnapshot = visibleMarketSnapshot(snapshot); return <PageFrame page="market" title="市場情報中心" description="以官方、可追溯資料快速掌握市場狀態；不提供投資建議。"><section className="market-hero"><div><p className="eyebrow">V5.4 Foundation</p><h2>資料時間優先於數字</h2><p>每項資料保留來源、資料時間與狀態。延遲、收盤、最近有效與取得失敗不混用。</p><p className="market-time-note">「市場資料時間」是來源資料的有效時間；「本次服務確認時間」只表示本次已向資料服務完成查詢，並非市場成交時間。</p></div><button type="button" onClick={onRefresh} disabled={isRefreshing}><RefreshCw size={16} className={isRefreshing ? 'is-spinning' : ''} aria-hidden="true" />{isRefreshing ? '更新中…' : '重新取得'}</button></section>{refreshMessage && <p className="market-alert" role="status">{refreshMessage}</p>}{visibleSnapshot.error && <p className="market-alert" role="status">{visibleSnapshot.error}</p>}{VISIBLE_MARKET_SECTIONS.map(group => <section className="market-section" key={group.key} aria-labelledby={`market-${group.key}`}><header><div><p className="eyebrow">{group.eyebrow}</p><h2 id={`market-${group.key}`}>{group.title}</h2><p>{group.description}</p></div></header><div className="market-card-grid">{visibleSnapshot.items.filter(item => item.group === group.key).map(item => <article className="market-data-card" key={item.id}><div className="market-card-heading"><small>{item.name}</small><b className={`market-status ${item.status}`}>{statusLabel(item.status)}</b></div><strong className={marketTone(item.change)}>{value(item)}</strong><span className={marketTone(item.change)}>{change(item)}</span><dl><div><dt>市場資料時間</dt><dd>{formatMarketTime(item.asOf)}</dd></div><div><dt>來源</dt><dd>{item.sourceUrl ? <a href={item.sourceUrl} target="_blank" rel="noreferrer">{item.source}</a> : item.source}</dd></div></dl>{item.detail && <p>{item.detail}</p>}</article>)}</div></section>)}<p className="market-disclaimer">市場資料僅供資訊整理，非即時交易報價、投資建議或報酬保證。</p></PageFrame>; }
