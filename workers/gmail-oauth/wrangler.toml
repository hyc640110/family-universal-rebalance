name = "universal-rebalance-gmail-oauth"
main = "src/index.ts"
compatibility_date = "2026-07-13"
[vars]
ENVIRONMENT = "preview"
ALLOWED_ORIGINS = "https://hyc640110.github.io"
ALLOWED_REDIRECTS = "https://hyc640110.github.io/family-universal-rebalance/preview/"

# Secrets intentionally have no values in source control. Configure a separate
# preview and production environment only after a durable session-store binding
# and Google OAuth redirect URIs have been reviewed.
